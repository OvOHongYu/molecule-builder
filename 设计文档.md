# 分子构建器原型设计文档

**项目**：有机化学反应工作台 — 分子构建器  
**文档类型**：原型设计文件  
**版本**：1.0  
**日期**：2026-09-12  
**状态**：原型设计稿

---

## 1. 概述

### 1.1 定位
分子构建器是“有机化学反应工作台”的核心输入模块，用于以**增量式、悬停驱动**的方式构建有机分子结构式。构建结果以“原子编号 + 连接键”的图结构存储，可自动排版为符合 IUPAC/ACS 标准的 2D 结构式，并自动命名、保存至有机分子库。

### 1.2 设计理念
- **碳端点为操作核心**：不依赖左侧原子面板，所有编辑通过悬停原子或键触发。
- **默认乙烷起步**：进入即有一条线、两个隐形碳端点，用户从已有结构生长。
- **悬停即出按钮**：原子悬停显示“增加 / 替换 / 删除”，键悬停显示“删除”。
- **结构式即标准**：渲染与导出符合国际标准，可直接用于论文、专利、教学。
- **图数据唯一性**：原子编号 + 连接键存储，正确区分同分异构体。

### 1.3 范围
- 支持常见有机分子：烷、烯、炔、醇、醛、酮、羧酸、酯、胺、苯系物等。
- 支持常见无机小分子作为端点基团：H₂O、HX、NH₃ 等（以原子/基团形式接入）。
- 不涉及聚合物、生物大分子、金属有机配合物。

---

## 2. 用户场景

| 场景 | 描述 |
|------|------|
| 快速搭建 | 从乙烷出发，连续增加碳链，构建正己烷 |
| 官能团引入 | 在碳端点替换为 -OH，得到醇 |
| 分支构建 | 在某碳上增加甲基，得到支链烷烃 |
| 不饱和键 | 将单键替换为双键，得到烯烃 |
| 删减修正 | 删除多余分支，保留主链 |
| 环化 | 通过首尾连接形成环，自动排版为多边形 |
| 命名与入库 | 实时查看 IUPAC 名，保存到分子库 |

---

## 3. 总体交互模型

### 3.1 默认状态

```
实际渲染：
——————————           两个端点为隐形碳原子

逻辑结构：
C1 — C2              atom 1: C, atom 2: C
                     bond 1: C1-C2, order=1
```

- 碳原子端点：不渲染字母，不渲染圆形，仅以键线交点表示。
- 每个原子有不可见圆形热区，半径约 0.6 倍键长。
- 每个键有不可见矩形热区，宽度约 0.4 倍键长。

### 3.2 悬停原子

```
              [+ 增加] [⇄ 替换] [× 删除]
                   |
            ——————●——————
```

- 工具条自动避让键线，优先显示在原子右上方。
- 若靠近画布边缘，自动翻转方向。
- 工具条按钮支持键盘 Tab 聚焦，回车触发。

### 3.3 悬停键

```
            ——————×——————
```

- 删除按钮显示在键中点上方。
- 点击后弹出“保留左侧 / 保留右侧 / 取消”。

---

## 4. 操作详细设计

### 4.1 增加操作

点击“+ 增加”后，弹出二级菜单，分三组。

#### 4.1.1 增加键

可选：单键、双键、三键、芳香键。

选择后：
1. 在当前原子对面自动生成一个**默认碳原子端点**。
2. 用所选键连接当前原子与新端点。
3. 新端点仍为隐形碳原子。
4. 自动排版：根据当前原子已有键数和杂化态，按标准键角放置新键。

示例：乙烷右端增加单键 → 丙烷

```
————————●——————
        \
         ●         新端点为隐形碳
```

#### 4.1.2 增加原子

可选：H、O、N、S、P、F、Cl、Br、I 等。

选择后：
1. 默认以**单键**连接当前原子与新原子。
2. 新原子作为端点渲染：显示元素符号，键线分离留出圆形空间。
3. 若选择 H，则显式渲染 H 和 C-H 键。
4. 校验价键、电荷、孤对电子。

#### 4.1.3 增加基团

可选：-OH、-NH₂、-COOH、-CHO、-NO₂、-SO₃H、-Ph、-CH₃ 等。

选择后：
1. 默认以**单键**连接当前原子与基团连接点。
2. 基团整体作为端点渲染，键线在基团连接原子处断开。
3. 基团内部按标准结构式排版。
4. 若基团连接点有多个可用价键，默认使用一个，其余保持隐式氢。

### 4.2 替换操作

点击“⇄ 替换”后，弹出原子/基团选择菜单。

规则：
1. 将当前端点原子替换为所选原子或基团。
2. **保留原有连接键**：原先连到当前原子的键，改为连到新原子或基团的连接点。
3. 替换后自动校验价键、电荷、孤对电子。
4. 若当前原子是碳且无连接键，则替换后新原子成为孤立端点。
5. 若替换为基团，则基团连接点承担原有连接。
6. 若替换导致价键超限，高亮提示并禁止确认。

示例：乙烷右端替换为 -OH → 乙醇

```
————————O—H
```

### 4.3 删除操作

#### 4.3.1 删除原子

悬停原子，点击“× 删除”。

**若当前原子是非碳原子**：
- 视为**替换为碳原子**。
- 移除该杂原子及其显式氢。
- 原连接键保留，连接点变为隐形碳端点。
- 不删除相邻结构。

**若当前原子是碳原子**：
- **无连接键**：直接删除该原子。
- **只有 1 个连接键**：删除该碳原子和该键；对侧原子成为新端点。
- **有 2 个或以上连接键**：弹出选择：
  - 删除左侧结构
  - 删除右侧结构
  - 取消

选择某一侧后：
1. 删除当前碳原子。
2. 删除所选一侧的所有原子和键。
3. 删除另一侧与当前碳原子直接相连的键。
4. 另一侧直接相连的原子成为新端点。
5. 若所选一侧没有其他分子结构，则只删除该侧键。
6. 若另一侧也没有其他分子结构，则删除该键后，孤立端点一并删除。
7. 若存在多个分支，按屏幕左右方向或分支列表让用户选择。

示例：删除中间碳，保留右侧

```
原结构：  ———●———●———●———
              ↑ 删除此碳，选择保留右侧
结果：    ———●———●———
```

#### 4.3.2 删除键

悬停键，点击“× 删除”。

弹出选择：
- 保留左侧
- 保留右侧
- 取消

选择后：
1. 删除该键。
2. 删除未保留一侧的所有原子和键。
3. 保留侧直接相连的原子成为新端点。
4. 若未保留一侧无其他结构，则只删除该键，并清理孤立端点。
5. 若删除的是环内键，则按开环处理：仅断开该键，不删除原子；若开环后产生不合法价键，提示用户。

---

## 5. 氢原子显示规则

### 5.1 全局开关
- 选项：“显示 C-H 氢” / “隐藏 C-H 氢”。
- 默认隐藏。
- 切换不改变分子图存储，仅改变渲染层。

### 5.2 隐藏时
- 碳上的氢为隐式氢，不渲染键和 H。
- 碳端点仍为隐形交点。
- 分子式、命名、反应预测按隐式氢计算。

### 5.3 显示时
- 根据碳的价键计算隐式氢数。
- 每个 H 作为非碳原子端点渲染：键线分离，留出圆形空间显示 H。
- C-H 键按标准单键渲染。
- 立体化学中的 H 按楔形/虚线楔形规则渲染。

---

## 6. 端点渲染规范

### 6.1 碳原子端点
- 不显示字母 C。
- 不显示圆形。
- 所有相连键的直线在碳原子中心相交。
- 若有显式 H，则 H 按非碳原子渲染。

### 6.2 非碳原子/基团端点
- 计算该原子所有相连键的方向。
- 在原子中心周围留出圆形空间，半径 R 根据元素符号或基团文本大小确定。
- 所有键线从圆周边缘开始，不进入圆内。
- 双键、三键的平行线同样在圆周处截断。
- 立体楔形键、虚线楔形键按 IUPAC/ACS 标准从圆周边缘开始渲染。
- 圆形区域填充白色，避免键线穿过文字。

示例：

```
碳端点：      ——————●——————       ●为隐形碳，实际不显示

非碳端点：    ——————( O )——————   键线在 O 周围断开
```

### 6.3 基团端点
- 基团连接原子按非碳原子规则渲染。
- 基团内部原子按标准结构式排版。
- 键线在基团连接原子圆周处截断。
- 基团文本缩写（Me、Et、Ph 等）按 IUPAC 推荐使用。

---

## 7. 自动排版规则

### 7.1 触发时机
- 每次增加、替换、删除后局部重新排版。
- 用户点击“自动排版”时全局重排。
- 用户手动拖动后，点击“自动排版”恢复标准几何。

### 7.2 几何规范
| 项目 | 规范 |
|------|------|
| 键长 | 统一，默认 1.5 单位 |
| sp³ 键角 | 约 109.5° |
| sp² 键角 | 约 120° |
| sp 键角 | 约 180° |
| 环 | 苯环正六边形，脂环正多边形 |
| 双键 | 两条平行线，间距约为键长 1/5 |
| 三键 | 三条平行线，间距均匀 |
| 线宽 | 普通键 0.6–0.8 pt |
| 原子间距 | 非键合原子间最小间距约 0.3 倍键长 |

### 7.3 新增原子方向选择
- 已有 1 个键：新键与现有键成约 109.5° 或 120°。
- 已有 2 个键：继续锯齿方向。
- 已有 3 个键：四面体投影。
- 已有 4 个键：四面体。
- 检测到闭环时，重排为规则多边形。

### 7.4 排版引擎
- 首选：RDKit `Compute2DCoords`。
- 备选：自研力导向 + 环模板。
- 导出 SVG 时保持矢量，缩放不失真。

---

## 8. 数据结构

### 8.1 原子（Atom）
| 字段 | 类型 | 说明 |
|------|------|------|
| atom_id | int | 分子内唯一编号，从 1 开始 |
| element | string | 元素符号 |
| charge | int | 形式电荷 |
| isotope | int | 同位素质量数，可空 |
| radical | int | 自由基电子数 |
| implicit_h | int | 隐式氢数 |
| stereo | string | 手性标记 R/S、E/Z |
| lone_pairs | int | 孤对电子数 |
| x, y | float | 渲染坐标，仅渲染层使用 |

### 8.2 键（Bond）
| 字段 | 类型 | 说明 |
|------|------|------|
| bond_id | int | 键唯一编号 |
| atom1_id | int | 起始原子编号 |
| atom2_id | int | 终止原子编号 |
| order | int | 键级：1、2、3、芳香 |
| stereo | string | 顺反、楔形键等 |
| aromatic | bool | 是否芳香键 |

### 8.3 分子图（MoleculeGraph）
```json
{
  "atoms": [
    {"atom_id": 1, "element": "C", "charge": 0, "implicit_h": 3},
    {"atom_id": 2, "element": "C", "charge": 0, "implicit_h": 3}
  ],
  "bonds": [
    {"bond_id": 1, "atom1_id": 1, "atom2_id": 2, "order": 1}
  ]
}
```

### 8.4 渲染层状态
```json
{
  "hover_atom_id": null,
  "hover_bond_id": null,
  "show_hydrogen": false,
  "atom_positions": {
    "1": {"x": 100, "y": 200},
    "2": {"x": 250, "y": 200}
  }
}
```

- 渲染层状态不影响分子图数据。
- 保存时仅提交分子图，坐标可选保存。

---

## 9. 与命名、保存、反应构建器的集成

### 9.1 实时命名
- 每次编辑后 300 ms 防抖计算。
- 输出：IUPAC 名、常用名、分子式、canonical SMILES、InChI、InChIKey。
- 复杂分子调用外部命名服务作为 fallback。

### 9.2 保存到分子库
- 保存时自动规范化，生成 InChIKey。
- 按 InChIKey 唯一索引去重。
- 若已存在，提示“该结构已在库中”，并显示已有分子链接。
- 保存字段：名称、分子式、SMILES、InChIKey、原子数组、键数组、标签、创建时间。

### 9.3 与反应构建器集成
- 分子可直接拖入反应构建器作为反应物。
- 反应构建器读取同一份分子图数据。
- 原子/键性质（电负性、极性、键能）由分子图推导。

---

## 10. UI 布局

```
┌────────────────────────────────────────────────────────────────────┐
│ 文件  编辑  视图  工具  帮助     [分子构建器]             用户菜单 │
├──────────────┬──────────────────────────────────┬──────────────────┤
│ 工具面板     │            中央画布              │ 属性面板         │
│              │                                  │                  │
│ 选择         │        SVG 结构式                │ 原子属性         │
│ 自动排版     │        悬停原子/键出按钮         │ 键属性           │
│ 撤销/重做    │        支持缩放、拖拽、框选      │ 分子信息         │
│              │                                  │ IUPAC 名称       │
│ 显示 C-H 氢  │                                  │ SMILES/InChIKey  │
│ 显示原子编号 │                                  │ 验证状态         │
│              │                                  │ [保存到库]       │
├──────────────┴──────────────────────────────────┴──────────────────┤
│ 状态：缩放 100% | 坐标 (x,y) | 提示 | InChIKey: XXXXXXXXXXXXXX     │
└────────────────────────────────────────────────────────────────────┘
```

- 左侧工具面板保持精简，仅放全局操作。
- 中央画布是主要工作区，所有编辑通过悬停触发。
- 右侧属性面板显示选中原子/键的详细信息和分子整体信息。

---

## 11. 状态机

```
                    ┌─────────────┐
                    │  默认乙烷   │
                    └──────┬──────┘
                           │ 悬停原子
                           ▼
                    ┌─────────────┐
              ┌─────│  原子工具条 │─────┐
              │     └─────────────┘     │
              ▼                         ▼
       ┌─────────────┐           ┌─────────────┐
       │  增加菜单   │           │  替换菜单   │
       └──────┬──────┘           └──────┬──────┘
              │                         │
              ▼                         ▼
       ┌─────────────┐           ┌─────────────┐
       │ 键/原子/基团│           │ 选择原子/基团│
       └──────┬──────┘           └──────┬──────┘
              │                         │
              ▼                         ▼
       ┌─────────────┐           ┌─────────────┐
       │ 生成新端点  │           │ 替换当前原子│
       └──────┬──────┘           └──────┬──────┘
              │                         │
              └──────────┬──────────────┘
                         ▼
                  ┌─────────────┐
                  │ 自动排版    │
                  │ 实时命名    │
                  └──────┬──────┘
                         │
                         ▼
                  ┌─────────────┐
                  │ 保存到分子库│
                  └─────────────┘
```

删除操作类似，根据原子/键类型进入不同分支。

---

## 12. 边界情况与错误处理

| 情况 | 处理 |
|------|------|
| 价键超限 | 高亮原子，提示“碳最多四键”，禁止确认 |
| 删除导致分子为空 | 重置为默认乙烷 |
| 删除环内键 | 开环处理，校验价键 |
| 替换导致电荷不平衡 | 提示并允许调整电荷 |
| 芳香键添加到非环结构 | 提示芳香键需在环中，或自动转为单键 |
| 多个分支删除 | 弹出分支列表让用户选择 |
| 画布边缘工具条溢出 | 自动翻转方向 |
| 手动拖动后排版混乱 | 提供“自动排版”按钮恢复 |
| InChIKey 重复 | 提示已存在，显示已有分子 |
| 命名失败 | 显示“命名失败”，允许手动输入 |

---

## 13. 技术实现建议

| 模块 | 技术 |
|------|------|
| 前端框架 | React + TypeScript |
| 渲染 | SVG 为主，Canvas 备选 |
| 化学引擎 | RDKit.js / RDKit |
| 自动排版 | RDKit Compute2DCoords + 自研环模板 |
| 命名 | 自研规则 + CACTUS/PubChem fallback |
| 状态管理 | Redux / Zustand |
| 后端 | FastAPI |
| 数据库 | PostgreSQL + JSONB |
| 导出 | SVG、PDF、PNG、MOL/SDF、SMILES、InChI |

---

## 14. 原型验收标准

1. 默认显示乙烷为一条线，两端无字母。
2. 悬停端点出现“增加/替换/删除”按钮。
3. 增加键时，对面默认生成隐形碳端点。
4. 增加原子/基团时，默认单键连接。
5. 替换端点时，保留原有连接键。
6. 删除碳原子时，按规则选择删除左侧或右侧结构。
7. 删除非碳原子时，视为替换为碳原子。
8. 删除键时，可选择保留左侧或右侧。
9. C-H 氢可全局显示或隐藏。
10. 非碳原子端点键线分离，留出圆形空间。
11. 自动排版符合 IUPAC/ACS 标准。
12. 实时显示 IUPAC 名称、分子式、InChIKey。
13. 保存时按 InChIKey 去重，避免同分异构体混淆。
14. 导出 SVG/PDF 可直接用于论文，键长、键角、立体化学符合标准。
15. 撤销/重做覆盖所有编辑操作。

---

## 15. 后续扩展

- 环化工具：首尾连接自动成环。
- 立体化学编辑：楔形键、R/S、E/Z。
- 模板库：常见骨架一键插入。
- 协作编辑：多人实时，原子级锁。
- 反应构建器联动：构建完成直接进入反应预测。
- 逆合成联动：从目标产物反推构建路径。

---

**文档结束**